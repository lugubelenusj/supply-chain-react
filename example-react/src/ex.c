
/**
 * calc.c
 *
 * Performs calculations on command line arguments with the following options:
 *
 * -a adds (sums) the arguments
 * -d divides the arguments
 * -m multiplies the arguments
 * -s substracts the arguments
 * -h prints help and usage information
 *
 */

int main(int argc, char *argv[])
{
    //TODO: use getopt to parse command line options. If the user specifies two
    // or more flags, you should probably print an error message. Also remember
    // to provide usage information.

    //TODO: apply the operation requested by the user

    //TODO: print the result
		int i = 0;
		printf("%d\n", i);
    return 0;
}